version: "3"

services:
  postgresql:
    container_name: postgres
    restart: always
    image: postgres:latest
    environment:
      POSTGRES_DB: "skladok"
      POSTGRES_USER: "root"
      POSTGRES_PASSWORD: "root"
    ports:
      - "5432:5432"

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: skladok-app
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/skladok
    ports:
      - "8080:8080"
    depends_on:
      - postgresql